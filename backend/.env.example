# Copy this file to .env and set values

# Database Configuration
DB_HOST=localhost           # localhost, ngrok tunnel, or cloud provider
DB_PORT=3306               # MySQL port (3306 default, or ngrok assigned port)
DB_USER=root               # MySQL username
DB_PASSWORD=               # MySQL password
DB_NAME=etusl_db           # Database name

# Server Configuration
PORT=4000                  # Node.js server port
NODE_ENV=development       # development or production
